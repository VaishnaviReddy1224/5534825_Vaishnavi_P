#include <stdio.h>

int main() {
    int count;
    scanf("%d", &count); 
    for (int i = 0; i < count; i++) {
        unsigned int num;
        scanf("%u", &num); 
        printf("%u\n", ~num); 
    }

    return 0;
}
